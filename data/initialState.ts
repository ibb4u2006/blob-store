import { iOptions } from "../utils/interface";

const initialState: iOptions = {
  primaryColor: "#f98305",
  borderRadius: 6,
  dismissable: false,
  dismissType: "text",
  expiration: 365,
};

export default initialState;
